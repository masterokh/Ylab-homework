package application;

import application.in.ConsoleProgram;

/**
 * The Main class serves as the entry point for the application.
 * It contains the main method that invokes the start method of the ConsoleProgram class.
 */
public class Main {
    /**
     * The main method that is the entry point of the application.
     * It calls the start method of the ConsoleProgram class to begin the execution.
     *
     * @param args The command-line arguments passed to the application.
     */
    public static void main(String[] args) {
        ConsoleProgram.start();
    }
}
